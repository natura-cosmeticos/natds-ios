/**
 Position is a enum that represents the icon/avatar side options for the chip component.
 
 These are all positions allowed for a icon/avatar in NatChip:
 - right
 - left
 */
extension NatChip {
    public enum Position {
        case left
        case right
    }
}
