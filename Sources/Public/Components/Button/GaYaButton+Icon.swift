extension GaYaButton {
    /**
     Position is a enum that represents the icon side options for the button component.
     
     These are all positions allowed for a icon in GaYaButton:
     - right
     - left
     */
    public enum Position {
        case left
        case right
    }
}
