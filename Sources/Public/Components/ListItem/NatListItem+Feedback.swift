extension NatListItemCell {
    /**
     FeedbackStyle is a enum that represents feedback styles for NatListItemCell component.
     
     These are all styles allowed:
     - ripple (adds a ripple animation)
     - selection (changes
     */
    public enum FeedbackStyle {
        case ripple
        case selection
    }
}
