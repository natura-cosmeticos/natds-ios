protocol PrimaryColorPalette {
    var primary: String { get }
    var onPrimary: String { get }

    var primaryDark: String { get }
    var onPrimaryDark: String { get }

    var primaryLight: String { get }
    var onPrimaryLight: String { get }
}
